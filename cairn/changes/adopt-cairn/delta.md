---
cairn: change
change: adopt-cairn
---

# Delta

## ADDED Requirements

The initial spec set is added wholesale: backends, commands, config, wizard and packaging. Their requirements describe calendula as it stands after this release, so the three behaviour changes landing alongside are folded into them directly rather than restated here.
