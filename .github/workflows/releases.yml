name: Releases

on:
  push:
    tags:
      - v*
    branches:
      - master

jobs:
  release:
    uses: pimalaya/nix/.github/workflows/releases.yml@master
    secrets: inherit
    with:
      project: calendula
      nixpkgs: nixos-25.11
